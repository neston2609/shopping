# Storefront dev server talks to the backend via the Vite proxy by default (/api).
# Override the proxy target if your backend runs elsewhere:
VITE_API_PROXY="http://localhost:9000"
# Or bypass the proxy and call the API directly (must allow CORS + credentials):
# VITE_API_URL="http://localhost:9000/api"
